# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET      /                          controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET      /assets/*file              controllers.Assets.at(path="/public", file)

# Tasks
## Lista las tareas existentes devolviéndolas como una colección de objetos JSON
GET      /tasks                     controllers.Application.tasks

## Devuelve un objeto JSON con la tarea solicitada
GET      /tasks/:id                 controllers.Application.getTask(id: Long)

## Crea una tarea nueva devolviéndola como objeto JSON en caso de éxito 
POST     /tasks                     controllers.Application.newTask

## Elimina la tarea indicada
DELETE   /tasks/:id                 controllers.Application.deleteTask(id: Long)